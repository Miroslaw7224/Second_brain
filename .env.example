# PORT: Server port (default 3000). Set to 3100 to run on http://localhost:3100
# PORT=3100

# GEMINI_API_KEY: Required for Gemini AI API calls.
# AI Studio automatically injects this at runtime from user secrets.
# Users configure this via the Secrets panel in the AI Studio UI.
GEMINI_API_KEY="MY_GEMINI_API_KEY"

# APP_URL: The URL where this applet is hosted.
# AI Studio automatically injects this at runtime with the Cloud Run service URL.
# Used for self-referential links, OAuth callbacks, and API endpoints.
APP_URL="MY_APP_URL"

# Google OAuth Credentials
# Obtain these from the Google Cloud Console (https://console.cloud.google.com)
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""

# Firebase (server / Admin SDK) - required for Auth + Firestore
# From Firebase Console > Project Settings > Service accounts (private key with \n as literal)
FIREBASE_PROJECT_ID=""
FIREBASE_CLIENT_EMAIL=""
FIREBASE_PRIVATE_KEY=""
# Optional: use Firestore instead of SQLite for documents/notes/chunks
USE_FIRESTORE="true or false"

# Firebase (client config - frontend) - REQUIRED for Firebase Auth
# From Firebase Console > Project Settings > General. Must be prefixed with VITE_ for Vite.
VITE_FIREBASE_API_KEY=""
VITE_FIREBASE_AUTH_DOMAIN=""
VITE_FIREBASE_STORAGE_BUCKET=""
VITE_FIREBASE_MESSAGING_SENDER_ID=""
VITE_FIREBASE_APP_ID=""
VITE_FIREBASE_MEASUREMENT_ID=""
